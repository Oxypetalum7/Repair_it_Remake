export class sprImg {
    constructor( img, width, height, x, y ) {
		this.img = new Image();
		this.img.src = img;
        this.width = width;
        this.height = height;
        this.x = x;
        this.y = y;
	}

    update( canvas ) {

	}

    render() {
    }
}
